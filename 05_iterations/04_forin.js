const myobj={
    js:'javscript',
    cpp:'C++',
    rb:"ruby",
    swift:"swift by apple"
}

for (const key in myobj) {
   //console.log(`${key} shortcut is for ${myobj[key]}`); //key likhte toh key print hota 
}

const arr=["js","rb","cpp"]
for (const key in arr) {
   // console.log(arr[key]); //key likhte toh key print hota 
    
}

//maps cannot be iterated using forin

